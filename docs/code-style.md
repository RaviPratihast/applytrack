# Code style

## File size

- **Target:** Keep each source file under **100 lines**.
- **Maximum:** Do not exceed **120 lines** per file.

When a file grows beyond this, split it (e.g. extract components, helpers, or constants into separate files) so the codebase stays easy to navigate and review.
